name: env demo

on:
  workflow_dispacth:

env:
  APP_NAME: netflix

jobs:
  demo:
    runs-on: ubuntu-latest
  env:
    APP_NAME: Amazon

  steps:
    - name: Print the job 
      run: echo $APP_NAME

    - name: print the second job
      env:
       APP_NAME:Flipcart
      run: echo $APP_NAME

    - name: print the three job
      run: $APP_NAME
