name: env demo

on:
  workflow_dispatch:

env:
  APP_NAME: netflix

jobs:
  demo:
    runs-on: ubuntu-latest

    env:
      APP_NAME: Amazon

    steps:
      - name: Print the job
        run: echo $APP_NAME

      - name: Print the second job
        env:
          APP_NAME: Flipcart
        run: echo $APP_NAME

      - name: Print the third job
        run: echo $APP_NAME
